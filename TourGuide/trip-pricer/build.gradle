// :trip-pricer build.gradle

bootWar {
    baseName = 'tourGuide-tripPricer'
    version =  '1.0.0'
}

dependencies {
    implementation project(':common')
    implementation fileTree(dir: 'src/main/resources/libs', include: 'TripPricer.jar')
}

jar {
    enabled = true
}

